[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "PORT=${PORT:-8000} && python manage.py migrate --noinput && gunicorn project.wsgi:application --bind 0.0.0.0:$PORT"
